
@{
    ViewData["Title"] = "LOC_CityAddList";
    Layout = "~/Views/Shared/_Layout.cshtml";
}

@model MultiUserAB.Areas.LOC_City.Models.LOC_CityModel

<div class="container-fluid row">
    <div class="col-md-12">
        @*<h3>@TempData["CityInsertMessage"]</h3>*@
        <form method="post" asp-area="LOC_City" asp-controller="Home" asp-action="Save">
            @Html.HiddenFor(x=>x.CityID)
            @**@
            <div class="mb-5">
                <label><span class="text-danger">*</span>Country Name</label>
                <select class="form-control" asp-for="CountryID" asp-items="@(new SelectList(ViewBag.CountryList,"CountryID","CountryName"))" onchange="onCountryChange();">
                    <option value="">Select Country</option>
                </select><span asp-validation-for="CountryID" class="text-danger"></span>
            </div>

            <div class="mb-5">
                <label><span class="text-danger">*</span>State Name</label>
                <select class="form-control" asp-for="StateID" asp-items="@(new SelectList(ViewBag.StateList,"StateID","StateName"))">
                    <option value="">Select State</option>
                </select><span asp-validation-for="StateID" class="text-danger"></span>
            </div>

            <div class="mb-5">
                <label><span class="text-danger">*</span>City Name</label>
                <input type="text" class="form-control" asp-for="CityName" placeholder="Enter the City Name" />
                <span asp-validation-for="CityName" class="text-danger"></span>
            </div>

            @Html.HiddenFor(x=>x.StateID)



            @*<div>
            <label>Created</label>
            <input type="datetime-local" class="form-control" asp-for="Created" />
            </div>

            <div>
            <label>Modified</label>
            <input type="datetime-local" class="form-control" asp-for="Modified" />
            </div>*@

            <hr />

            <input type="submit" class="btn btn-success" />
            <a type="submit" class="btn btn-danger" value="Cancel" asp-area="LOC_State" asp-controller="Home" asp-action="Index">Cancel</a>
        </form>
    </div>
</div>


<script>
    function onCountryChange() {
        var CountryID = $("#CountryID").val();

        if (CountryID !== "") {
            $("#StateID").empty();
            $("#StateID").append($("<option></option>").val("").html("Select State"));
            $.ajax(
                {
                    type: "POST",
                    contantType: "application/json; charset=utf-8",
                    url: "@Url.Action("DropDownByCountry", "Home")" + "?&CountryID=" + CountryID,
                    data: {},
                    dataType: "json",
                    success: function (Result) {
                        $.each(Result, function (key, value) {
                            $("#StateID").append($("<option></option>").val(value.stateID).html(value.stateName));
                        });
                    },
                    error: function (r) {
                        alert("Error while loading!!");
                    }
                });
        } else {
            $("#StateID").empty();
            $("#StateID").append($("<option></option>").val("").html("Select State"));
        }
    }
</script>

@section Scripts{
    @{
        await Html.RenderPartialAsync("_ValidationScriptsPartial");
    }
}